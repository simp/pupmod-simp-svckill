---
fixtures:
  symlinks:
    svckill: "#{source_dir}"
    common: "#{source_dir}/../common"
    simplib: "#{source_dir}/../simplib"
    simpcat: "#{source_dir}/../concat"
    stdlib: "#{source_dir}/../stdlib"
