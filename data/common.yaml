---
svckill::ignore_defaults:
  - auditd
  - puppet
  - puppetmaster
  - crond
  - sshd
# exclude all PE services
  - ^pe-.*
  - simp_client_bootstrap
lookup_options:
  svckill::ignore_defaults:
    merge: unique
